# Sorting Visualizer

A small vanilla JS/HTML/CSS tool for visualizing how sorting algorithms move
through an array. Made for my DSA class to actually *see* the difference
between algorithms instead of just tracing pseudocode on paper.

Open `index.html` in a browser — no build step, no dependencies.

More algorithms coming as we cover them in class.
